# Day 59

Notes reading [Doc 1](../Doc001.md).

* p.431
  - extended register instructions
    - optional sign-extension or zero-extension of a portion of the second source register value

* p.432
  - Arithmetic with carry instructions

* p.436
  - `CRC32` instructions
    - `CRC32B`: `CRC-32` sum from byte
    - `CRC32H`: `CRC-32` sum from halfword
    - `CRC32W`: `CRC-32` sum from word
    - `CRC32X`: `CRC-32` sum from doubleword
    - `CRC32CB`: `CRC-32C` sum from byte
    - `CRC32CH`: `CRC-32C` sum from halfword
    - `CRC32CW`: `CRC-32C` sum from word
    - `CRC32CX`: `CRC-32C` sum from doubleword

* p.437
  - Conditional select instructions

* p.438
  - Conditional comparison instructions

* p.439
