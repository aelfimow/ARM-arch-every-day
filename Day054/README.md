# Day 54

Notes reading [Doc 1](../Doc001.md).

* p.371
  - instructions that add a Pointer Authentication Code (`PAC`) to the address in a register

* p.372
  - instructions that authenticate a `PAC` in a register

* p.373
  - instructions that strip a `PAC` from a register, without performing any authentication

* p.376
  - Note: In some cases, load/store instructions can lead to `CONSTRAINED UNPREDICTABLE` behavior
