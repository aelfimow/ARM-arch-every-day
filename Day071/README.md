# Day 71

Notes reading [Doc 1](../Doc001.md).

* p.531
  - `SME2` Integer minimum/maximum
  - `SME2.2` `BFloat16` multi-vector multiply instructions
  - `SME2.2` Floating-point multi-vector multiply instructions

* p.532
  - `SME2` `BFloat16` multiply-accumulate instructions
  - `SME2` Floating-point multi-vector multiply-accumulate instructions

* p.533
  - `SME2` `FP8` floating-point widening multiply-accumulate instructions
  - `SME2` Integer multi-vector multiply-accumulate instructions

* p.534
  - `SME2` Multi-vector multiply high

* p.535
  - `SME2` Multi-vector select
  - `SME2` Multi-vector shift and convert instructions

* p.536
  - `SME` `BFloat16` outer product instructions
  - `SME2` Binary outer product instructions
  - `SME` Floating-point outer product instructions

* p.537
  - `SME2` `FP8` floating-point outer product instructions
  - `SME` Integer outer product instructions
  - `SME` `BFloat16` quarter-tile outer product instructions

* p.538
  - `SME` floating-point quarter-tile outer product instructions
  - `SME2` `FP8` floating-point quarter-tile outer product instructions
  - `SME` Integer quarter-tile outer product instructions

* p.539
  - `SME` `BFloat16` sparsity outer product instructions
  - `SME` Floating-point sparsity outer product instructions
  - `SME2` `FP8` floating-point sparsity outer product instructions

* p.540
  - `SME` Integer sparsity outer product instructions
  - `SME` Stack frame instructions
  - `SME2` Unpack and extend instructions

* p.541
  - `SME`/`SME2` Zero instructions
  - `SME2.1` Zero `ZA` array vectors instructions

* p.542
