# Day 1

Notes reading [Doc 1](../Doc001.md).

* p.23
  - `PE`: Processing element

* p.23
  - Execution state: `AArch32`
  - `AArch32` state: `T32`, `A32` instruction sets
  - Execution state: `AArch64`
  - `AArch64` state: `A64` instruction set
  - ARM A architecture profile:
    - Armv8-A
    - Armv9-A
  - `VMSA`: Virtual Memory System Architecture

* p.23:
  - Note: In `AArch32` state, execution at `EL0` is execution in `User mode`

* p.24:
  - Note: `User mode` is the only mode where software execution is unprivileged
  - `DCC`: Debug Communications Channel

* p.33:
  - `R`: Rule
  - `I`: Information statemen
  - `S`: Software usage statement
  - `D`: Declaration

* p.39
