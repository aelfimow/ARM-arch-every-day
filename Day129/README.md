# Day 129

Notes reading [Doc 1](../Doc001.md).

* p.2133
  - `CRC32B`, `CRC32H`, `CRC32W`, `CRC32X`: `CRC32` checksum
    - the polynomial `0x04C11DB7` is used for the `CRC`

* p.2135
  - `CRC32CB`, `CRC32CH`, `CRC32CW`, `CRC32CX`: CRC32C checksum
    - the polynomial `0x1EDC6F41` is used for the `CRC`
