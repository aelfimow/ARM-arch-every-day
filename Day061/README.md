# Day 61

Notes reading [Doc 1](../Doc001.md).

* p.457
  - `SIMD` `FP8` floating-point conversion instructions
  - `SIMD` reduce (across vector lanes) instructions

* p.458
  - `SIMD` pairwise arithmetic instructions
  - Integer dot product
    - Vector form
    - Indexed form

* p.459
  - `SIMD` integer dot product instructions
  - `SIMD` `FP8` floating-point dot product instructions
  - Integer matrix multiply-accumulate

* p.460
  - `SIMD` integer matrix multiply-accumulate instructions
  - `SIMD` `FP8` matrix multiply-accumulate instructions

* p.461
  - `SIMD` table vector lookup instructions
  - `SIMD` lookup table using 2-bit or 4-bit indices instructions
  - `SIMD` complex number arithmetic instructions

* p.462
  - `SIMD` floating-point fused multiply-accumulate instructions
  - `SIMD` `FP8` floating-point widening multiply-accumulate instructions

* p.463
  - `SIMD` `BFloat16` floating-point multiply-add instructions
  - `SIMD` `BFloat16` floating-point dot product instructions
  - `SIMD` `BFloat16` floating-point matrix multiply instructions

* p.464
  - `SIMD` `BFloat16` floating-point convert instructions
  - Advanced Encryption Standard: `AES`
  - Secure Hash Algorithm: `SHA`
  - The Galois/Counter Mode of Operation
  - Cryptographic Extension instructions
    - `AESD`: `AES`single round decryption
    - `AESE`: `AES`single round encryption
    - `AESIMC`: `AES`inverse mix column 
    - `AESMC`: `AES`mix columns
    - `PMULL`: Polynomial multiply long `PMULL`, `PMULL2`
    - `SHA1C`: `SHA1`hash update (choose)
    - `SHA1H`: `SHA1`fixed rotate
    - `SHA1M`: `SHA1`hash update (majority)
    - `SHA1P`: `SHA1`hash update (parity)
    - `SHA1SU0`: `SHA1` schedule update 0
    - `SHA1SU1`: `SHA1` schedule update 1
    - `SHA256H`: `SHA256` hash update, part 1
    - `SHA256H2`: `SHA256` hash update, part 2 
    - `SHA256SU0`: `SHA256` schedule update 0
    - `SHA256SU1`: `SHA256` schedule update 1

* p.465
